// ============================================
// 🔧 Configuration - TEMPLATE
// ============================================
// Client-side config only. Do NOT place secrets here.
// Secret keys must stay in Vercel server environment variables.

const CONFIG_SETTINGS = {
    // ===========================================
    // ⚙️ FEATURE TOGGLES
    // ===========================================
    ENABLE_VLM: true,
    ENABLE_MEME_SEARCH: true,
};

window.APP_CONFIG = CONFIG_SETTINGS;
